import styled from "styled-components";

const Header = styled.div`
  position: absolute;
  top: 16px;
  left: 16px;
  color: #fff;
  font-size: 1.5rem;
  font-weight: 600;
  z-index: 10;
`;

export default Header;
